import { CvDirective } from './cv.directive';

describe('CvDirective', () => {
  it('should create an instance', () => {
    const directive = new CvDirective();
    expect(directive).toBeTruthy();
  });
});
