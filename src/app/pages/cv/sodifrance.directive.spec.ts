import { SodifranceDirective } from './sodifrance.directive';

describe('SodifranceDirective', () => {
  it('should create an instance', () => {
    const directive = new SodifranceDirective();
    expect(directive).toBeTruthy();
  });
});
